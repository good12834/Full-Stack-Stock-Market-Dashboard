// Old Sidebar is no longer used (replaced by IconRail + TopBar in App.jsx).
// Kept as a no-op re-export so older imports don't break.

import React from 'react';
const Sidebar = () => null;
export default Sidebar;
